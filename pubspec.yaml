name: responsive_web_layout
description: A Package created for building dynamic and responsive UI for Web Platforms.
version: 0.0.1
repository: https://github.com/tratum/responsive-layout/tree/responsive-web-layout
issue_tracker: https://github.com/tratum/responsive-layout/issues
homepage: https://github.com/tratum/responsive-layout
documentation: https://github.com/tratum/responsive-layout/tree/responsive-web-layout
topics:
  - widgets
  - UI
  - layout
  - web

environment:
  sdk: '>=3.1.5 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: